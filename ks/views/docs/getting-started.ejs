@extends('layout')

@section('content')
<div class="docs-page">
  <div class="docs-header">
    <h1>Getting Started</h1>
    <p class="lead">Follow these steps to get up and running quickly.</p>
  </div>

  <div class="docs-content">
    <h2>Prerequisites</h2>
    <p>Before you begin, make sure you have the following installed:</p>
    <ul>
      <li>Node.js v18 or higher</li>
      <li>npm or yarn package manager</li>
      <li>A code editor (VS Code recommended)</li>
    </ul>

    <h2>Installation</h2>
    <p>Install the package using npm:</p>
    <pre><code>npm install ks-docs-website</code></pre>

    <h2>Basic Setup</h2>
    <p>Create a new file called <code>server.js</code> and add the following:</p>
    <pre><code>const express = require('express');
const app = express();

app.set('view engine', 'ejs');
app.set('views', './views');

app.get('/', (req, res) => {
  res.render('dashboard');
});

app.listen(3000, () => {
  console.log('Server running on port 3000');
});</code></pre>

    <h2>Running the Server</h2>
    <p>Start your development server:</p>
    <pre><code>node server.js</code></pre>
    <p>Then navigate to <code>http://localhost:3000</code> in your browser.</p>
  </div>
</div>
@endsection