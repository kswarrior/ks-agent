@extends('layout')

@section('content')
<div class="docs-page">
  <div class="docs-header">
    <h1>Configuration</h1>
    <p class="lead">Learn how to configure and customize your setup.</p>
  </div>

  <div class="docs-content">
    <h2>Configuration File</h2>
    <p>The main configuration file is <code>config.json</code>. Here's a sample configuration:</p>
    <pre><code>{
  "app": {
    "name": "KS Docs",
    "env": "production",
    "port": 3000
  },
  "database": {
    "host": "localhost",
    "port": 5432,
    "name": "ks_docs"
  },
  "features": {
    "search": true,
    "darkMode": true,
    "analytics": true
  }
}</code></pre>

    <h2>Environment Variables</h2>
    <p>You can also set configuration using environment variables:</p>
    <ul>
      <li><code>PORT</code> — Server port (default: 3000)</li>
      <li><code>NODE_ENV</code> — Environment mode</li>
      <li><code>DATABASE_URL</code> — Database connection string</li>
    </ul>

    <h2>Feature Flags</h2>
    <p>Enable or disable features by modifying the <code>features</code> section in your configuration.</p>
  </div>
</div>
@endsection